# Configurações do PostgreSQL (Supabase)
spring.datasource.url=jdbc:postgresql://aws-0-sa-east-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.eqxskcklrytovvufbtly
spring.datasource.password=wOqdEb9ruJTPwlFx
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configurações adicionais (opcional)
spring.sql.init.mode=always
spring.datasource.max-active=10
spring.datasource.max-idle=5
spring.datasource.min-idle=2
spring.datasource.max-wait=10000

# Log de nível de SQL (opcional para debugging)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

debug=true
server.port=8080
